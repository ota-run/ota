version: 1
project:
  name: full-contract
  description: Full example of the current Ota contract surface
  type: application
tools:
  cargo: '*'
tasks:
  build:
    run: cargo build
  test:
    run: cargo test
    safe_for_agent: true
