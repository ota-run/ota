{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://dist.ota.run/spec/json-schemas/latest/explain.json",
  "title": "ota explain --json",
  "type": "object",
  "additionalProperties": false,
  "required": ["ok", "path", "summary", "actions", "steps"],
  "properties": {
    "ok": { "type": "boolean" },
    "path": { "type": "string" },
    "summary": {
      "type": "object",
      "additionalProperties": false,
      "required": ["error_count", "warn_count", "info_count", "step_count"],
      "properties": {
        "error_count": { "type": "integer", "minimum": 0 },
        "warn_count": { "type": "integer", "minimum": 0 },
        "info_count": { "type": "integer", "minimum": 0 },
        "step_count": { "type": "integer", "minimum": 0 }
      }
    },
    "actions": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "action_key", "action_title", "severity", "count", "why", "next"],
        "properties": {
          "order": { "type": "integer", "minimum": 1 },
          "action_key": { "type": "string" },
          "action_title": { "type": "string" },
          "severity": { "enum": ["error", "warn", "info"] },
          "count": { "type": "integer", "minimum": 1 },
          "why": { "type": "string" },
          "next": { "type": "string" },
          "commands": {
            "type": "array",
            "items": { "type": "string" }
          },
          "provenance": { "type": "string" }
        }
      }
    },
    "steps": {
      "type": "array",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["order", "code", "severity", "summary", "why", "next"],
        "properties": {
          "order": { "type": "integer", "minimum": 1 },
          "code": { "type": "string" },
          "severity": { "enum": ["error", "warn", "info"] },
          "summary": { "type": "string" },
          "why": { "type": "string" },
          "next": { "type": "string" },
          "provenance": { "type": "string" },
          "provenance_key": {
            "enum": ["org_policy", "repo_signals", "repo_contract"]
          }
        }
      }
    }
  }
}
