🦦 DOCTOR ./ota.yaml

➤ BLOCKED

Verdict
 ●  Repo: blocked
 ●  Agent: ready

➤ Primary Blocker Version mismatch for runtime: node (1)
Why: node resolved to `24.14.1` but the contract requires `22`; ota probed
     `./bin/node` with `node --version`
Provenance: repo contract
Next:
  » run `mise install node@22`
  » rerun `ota doctor`

Execution
 »  Mode: `native`
 »  Lifecycle: `ephemeral`

AGENT

Overview
 »  Posture: `readiness_strict`
 »  Entrypoint: `ci`
 »  Default task: `ci`

Execution
Safe tasks (1): `ci`
Verify after changes (1): `ci`

Boundary
Writable paths (2): `src/`, `docs/`
Protected paths (2): `ota.yaml`, `Cargo.lock`

◉ WARN  Ephemeral lifecycle is advisory in native mode (1)
Why: `execution.lifecycle: ephemeral` is advisory in native mode only. Native execution
     still runs in the host shell.
Provenance: repo contract
Next: use `ota run <task>` for isolated execution; use `ota up` for readiness only

◉ WARN  task `ci` uses replaceable shell `run` instead of `command` (1)
Why: task `ci` keeps a finite argv-shaped command in shell `run` at `tasks.ci.run`
     (`echo ci`), which is less governable and less inspectable than the first-class
     `command` surface
Provenance: repo contract
Next: replace `tasks.ci.run` with `tasks.ci.command` using `exe: echo` and
      `args: [`ci`]`; reserve `run`/`script` for shell behavior that cannot be modeled
      structurally