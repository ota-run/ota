🦦 WORKSPACE VALIDATE ./ota.workspace.yaml

➤ VALID

Next:
  ▸ run `ota workspace doctor` to inspect readiness
  ▸ run `ota workspace up` to prepare the workspace end to end
  ▸ run `ota workspace tasks` to inspect runnable task usage