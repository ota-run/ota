DOCTOR ./ota.yaml

NOT READY

Verdict
 -  Repo: not ready
 -  Agent: ready

-> Primary Blocker
Version mismatch for runtime: node
Why: node resolved to `24.14.1` but the contract requires `22`
Next: run `nvm install 22 && nvm use 22` and rerun `ota doctor`

Execution
 -  Mode: `native`
 -  Lifecycle: `ephemeral`

Agent
 -  Entrypoint: `ci`
 -  Default task: `ci`
 -  Safe tasks: `ci`
 -  Verify after changes: `ci`
 -  Writable paths: `src`, `docs`
 -  Protected paths: `ota.yaml`, `Cargo.lock`

WARN  Ephemeral lifecycle is advisory in native mode
Why: `execution.lifecycle: ephemeral` is advisory in native mode only. Native execution still runs in the host shell.
Next: use `ota run <task>` for isolated execution; use `ota up` for readiness only
