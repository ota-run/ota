🦦 UP ./ota.yaml

✓ READY
Phase: post-up diagnosis
Backend: native

◉ WARN  Ephemeral lifecycle is advisory in native mode (1)
Why: `execution.lifecycle: ephemeral` is advisory in native mode only. Native execution still runs in the host shell.
Next: use `ota run <task>` for isolated execution; use `ota up` for readiness only

🦦 UP SUMMARY

Status:      success
Scope:       repo
Path:        .
Contract:    ./ota.yaml
Mode:        native
Task:        post-up diagnosis
Lifecycle:   ephemeral
Note:        running on the host environment; declared lifecycle `ephemeral` has no managed effect in native mode

Next:
  » use `ota run <task>` for isolated execution
  » use `ota up` for readiness only
