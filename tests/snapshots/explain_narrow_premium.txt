🦦 EXPLAIN ./ota.yaml

➤ BLOCKED

Plan
 1. Install required runtimes and tools (2)
  Why:
    » node resolved to `24.14.1` but the
      contract requires `22`
    » ota probed
      `./bin/node` with
      `node --version`
    » npm is declared in the contract but is not
      available on PATH
  Provenance: repo contract
  Next: install or align the listed runtimes and
        tools, then rerun `ota doctor`

 2. task `ci` uses replaceable shell `run` instead of `command` (1)
  Why: task `ci` keeps a finite argv-shaped
       command in shell `run` at `tasks.ci.run`
       (`echo ci`), which is less governable and
       less inspectable than the first-class
       `command` surface
  Provenance: repo contract
  Next: replace `tasks.ci.run` with
        `tasks.ci.command` using `exe: echo` and
        `args: [`ci`]`; reserve `run`/`script`
        for shell behavior that cannot be
        modeled structurally

Overview
 »  Findings: 2
 »  Actions: 2
 »  Errors: 2
 »  Warnings: 1
 »  Info: 0