🦦 EXPLAIN ./ota.yaml

➤ NOT READY

Plan
 1. Fix version mismatches (2)
  Why: one or more runtime or tool entries do
    not match the contract
  » node resolved `24.14.1`, requires `22`
    Next: install a compatible node version that satisfies `22`, then rerun `ota doctor`
  » npm resolved `24.14.1`, requires `10`
    Next: install a compatible npm version that satisfies `10`, then rerun `ota doctor`

 2. Review contract drift (2)
  Why: repo signals no longer match the declared
    contract
  » `runtimes.node` is no longer detected
  » `tools.npm` is no longer detected
  Next:
    run `ota detect --merge --dry-run`
    to review the comparison, then
    `ota detect --merge`

Overview
 »  Findings: 4
 »  Actions: 2
 »  Errors: 2
 »  Warnings: 2
 »  Info: 0
