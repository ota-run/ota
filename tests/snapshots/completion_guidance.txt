$ ota completion bash
Auto setup:
ota completion bash --setup

Manual setup (~/.bashrc):
# >>> ota completion >>>
if command -v ota >/dev/null 2>&1; then
  source <(COMPLETE=bash ota)
fi
# <<< ota completion <<<
Reload your shell after updating ota so completions stay in sync.

---

$ ota completion zsh
Auto setup:
ota completion zsh --setup

Manual setup (~/.zshrc):
# >>> ota completion >>>
if command -v ota >/dev/null 2>&1; then
  if ! whence compdef >/dev/null 2>&1; then
    autoload -Uz compinit
    compinit
  fi
  source <(COMPLETE=zsh ota)
fi
# <<< ota completion <<<
Reload your shell after updating ota so completions stay in sync.

---

$ ota completion fish
Auto setup:
ota completion fish --setup

Manual setup (~/.config/fish/completions/ota.fish):
# >>> ota completion >>>
if type -q ota
    COMPLETE=fish ota | source
end
# <<< ota completion <<<
Reload your shell after updating ota so completions stay in sync.

---

$ ota completion powershell
Auto setup:
ota completion powershell --setup

Manual setup ($PROFILE):
# >>> ota completion >>>
if (Get-Command ota -ErrorAction SilentlyContinue) {
  $env:COMPLETE = "powershell"
  ota | Out-String | Invoke-Expression
  Remove-Item Env:\COMPLETE -ErrorAction SilentlyContinue
}
# <<< ota completion <<<
Reload your shell after updating ota so completions stay in sync.

---

$ ota completion elvish
Auto setup:
ota completion elvish --setup

Manual setup (~/.elvish/rc.elv):
# >>> ota completion >>>
if (has-external-command ota) {
  eval (E:COMPLETE=elvish ota | slurp)
}
# <<< ota completion <<<
Reload your shell after updating ota so completions stay in sync.
