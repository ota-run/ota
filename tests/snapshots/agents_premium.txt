🦦 AGENTS ./ota.yaml

Target:
./AGENTS.md

Managed block:
Ota-generated content

Next:
  » run `ota agents --write` to write `./AGENTS.md`
  » run `ota doctor` to verify repo readiness and task safety from the same contract

# AGENTS.md

Generated from `./ota.yaml` by `ota agents`.

## Repo

- `project`: `existing`

## Agent Contract

No explicit `agent` block is declared in `ota.yaml` yet.

Suggested next commands:

- `ota tasks`
- `ota doctor`
- `ota detect --dry-run`
- `ota init --bootstrap`